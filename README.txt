RE-ACCESSORIZE GITHUB PAGE

Upload index.html and the complete images folder to the root of the GitHub Pages repository.

The page has no build step or external JavaScript dependencies. Google Fonts are loaded from the web; the page falls back to system fonts if they are unavailable.

Before final submission, add any portfolio or project links required by the module and confirm the public GitHub Pages URL in the pitch deck and digital sketchbook.
